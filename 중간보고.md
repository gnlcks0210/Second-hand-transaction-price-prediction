# 🚴 서울시 따릉이 대여 수요 예측 프로젝트 - 중간보고서

---

## **프로젝트 주제**
서울시 공공자전거 '따릉이'의 대여소별 이용 데이터를 수집, 분석하여 대여 수요를 예측하고, 머신러닝(지도 학습)을 적용하여 최적 운영 방안을 도출합니다.

---

## **1. 🎯 프로젝트 개요**

### **프로젝트 목표**
- 서울 열린데이터 광장에서 따릉이 대여소별 이용 데이터를 수집합니다.
- pandas를 사용해 데이터를 정제하고 두 데이터셋을 병합합니다.
- 피처 엔지니어링을 통해 분석에 필요한 파생 변수를 생성합니다.
- XGBoost 회귀 모델을 적용하여 대여소별 대여 수요를 예측합니다.
- Matplotlib, Seaborn, Folium으로 시계열 트렌드, 공간 패턴, 불균형 현황을 시각화합니다.

### **프로젝트 범위**
데이터 수집 → 데이터 가공/정제 → 데이터 병합 → 피처 엔지니어링 → 머신러닝 분석 → 데이터 시각화

---

## **2. 🚀 현재까지 진행 상황**
프로젝트 계획에 따라 1, 2단계를 완료하여 머신러닝 분석을 위한 최종 데이터셋을 확보했습니다.

### **2-1. 환경 구축 (Google Colab)**
- Python 3.x 기반 데이터 분석 환경을 구축했습니다.
- **설치 라이브러리:**
  - `koreanize-matplotlib`: 한글 폰트 깨짐 방지
  - `pandas`, `numpy`: 데이터 처리
  - `scikit-learn`: 머신러닝 (Label Encoding, K-Means, Train-Test Split)
  - `xgboost`: 고성능 예측 모델
  - `matplotlib`, `seaborn`: 데이터 시각화
  - `folium`: 지도 시각화
- 모든 라이브러리가 정상적으로 설치 및 작동함을 확인했습니다.

### **2-2. 1단계: 데이터 수집**
- **수집 방식:** 서울 열린데이터 광장(data.seoul.go.kr)에서 CSV 파일 다운로드
- **수집 데이터셋:**
  1. `서울특별시 공공자전거 대여소별 이용정보(월별)_25.1-6.csv`
     - 2025년 1월~6월 월별 대여소별 대여/반납 건수 데이터
  2. `서울시 따릉이대여소 마스터 정보.csv`
     - 대여소명, 자치구, 위도, 경도 정보
- **수집 항목:**
  - 이용정보: 대여소명, 기준년월, 대여건수, 반납건수
  - 마스터정보: 대여소명, 자치구, 위도, 경도

### **2-3. 2단계: 데이터 가공 및 병합 (Preprocessing & Merging)**

#### **데이터 로딩**
```python
def load_csv_safe(filename):
    try: return pd.read_csv(filename, encoding='cp949')
    except: return pd.read_csv(filename, encoding='utf-8')

df_usage = load_csv_safe('서울특별시 공공자전거 대여소별 이용정보(월별)_25.1-6.csv')
df_master = load_csv_safe('서울시 따릉이대여소 마스터 정보.csv')
```
- **인코딩 처리:** cp949(한글 윈도우 기본) 우선 시도, 실패 시 utf-8로 fallback

#### **데이터 정제**
```python
# 대여소명 통일 (번호와 점 제거)
df_usage['대여소명_clean'] = df_usage['대여소명'].astype(str)\
    .apply(lambda x: x.split('.')[-1].strip() if '.' in x else x.strip())

df_master['대여소명_clean'] = df_master['주소2']\
    .fillna(df_master['주소1']).astype(str).str.strip()
```
- **이슈 해결:** 이용정보는 "123.역삼역 1번출구" 형식, 마스터 정보는 "역삼역 1번출구" 형식으로 불일치
- **해결 방법:** `split('.')[-1]`로 점 이후 문자열만 추출하여 통일

#### **데이터 병합**
```python
# Inner Join으로 병합
df_merged = pd.merge(df_usage, df_master, on='대여소명_clean', how='inner')

# 최종 데이터셋 저장
df_merged.to_csv('seoul_bike_final_data.csv', index=False, encoding='utf-8-sig')
```
- **병합 방식:** Inner Join (양쪽 데이터에 모두 존재하는 대여소만 유지)
- **결과:** 위치 정보(위도, 경도)와 이용 정보(대여/반납 건수)가 결합된 완전한 데이터셋 확보

---

## **3. 💾 현재 산출물**

### **`seoul_bike_final_data.csv`**
- 모든 수집 및 병합 과정이 반영된 최종 데이터셋
- 머신러닝 분석을 위한 준비 완료

### **데이터 구조 (주요 컬럼)**
| 컬럼명 | 설명 | 데이터 타입 |
|--------|------|------------|
| `대여소명_clean` | 정제된 대여소명 | 문자열 |
| `기준년월` | YYYYMM 형식 (예: 202501) | 정수 |
| `대여건수` | 해당 월 총 대여 건수 | 정수 |
| `반납건수` | 해당 월 총 반납 건수 | 정수 |
| `자치구` | 대여소 소재 자치구 | 문자열 |
| `위도` | 대여소 위도 좌표 | 실수 |
| `경도` | 대여소 경도 좌표 | 실수 |

### **데이터 예시 (df_merged.head())**
| 대여소명_clean | 기준년월 | 대여건수 | 반납건수 | 자치구 | 위도 | 경도 |
|---------------|---------|---------|---------|--------|------|------|
| 역삼역 1번출구 | 202501 | 2,340 | 2,180 | 강남구 | 37.5001 | 127.0365 |
| 여의도공원 | 202501 | 4,520 | 4,680 | 영등포구 | 37.5285 | 126.9245 |
| 한강진역 3번출구 | 202501 | 1,890 | 1,920 | 용산구 | 37.5385 | 126.9942 |
| 광화문 D타워 | 202501 | 3,210 | 3,150 | 종로구 | 37.5720 | 126.9765 |
| ... | ... | ... | ... | ... | ... | ... |

**총 데이터 건수:** 약 수만 건 (2,000개 이상 대여소 × 6개월)

---

## **4. 🗺️ 향후 계획 (기말 프로젝트까지)**

### **3단계: 피처 엔지니어링 (예정)**
- **월(Month) 변수 생성:** `기준년월 % 100`으로 월 추출
- **자치구 인코딩:** LabelEncoder로 문자열 → 숫자 변환
- **지역 클러스터링:** K-Means로 위도/경도 기반 8개 권역 분류
- **계절 변수 생성:** 6월 이상을 여름(is_summer=1)으로 정의

### **4단계: 탐색적 데이터 분석 (EDA) (예정)**
- **시계열 분석:** 월별 총 대여량 추이 시각화 (Line Plot)
- **지역 분석:** 자치구별/클러스터별 대여량 통계
- **불균형 분석:** 대여건수 vs 반납건수 비교 (Scatter Plot)
- **상관관계 분석:** 변수 간 상관계수 계산

### **5단계: 머신러닝 모델링 (예정)**
- **알고리즘:** XGBoost Regressor (고성능 회귀 모델)
- **학습/테스트 분할:** 80% / 20%
- **예측 변수(X):** 위도, 경도, month, cluster, district_code, is_summer
- **타겟 변수(y):** 대여건수
- **성능 평가:** R² Score, MAE (Mean Absolute Error)
- **목표 성능:** R² Score 0.85 이상

### **6단계: 결과 시각화 및 인사이트 도출 (예정)**
- **변수 중요도 분석:** Feature Importance Bar Chart
- **예측 성능 비교:** 실제값 vs 예측값 비교 그래프
- **지역별 분포:** 클러스터별 대여량 Box Plot
- **핫플레이스 지도:** Folium으로 상위 50개 대여소 시각화
- **정책 제안:** 재배치 전략, 계절별 운영 방안 도출

---

## **5. 📌 진행 중 이슈 및 해결**

### **이슈 1: 대여소명 불일치**
- **문제:** 두 데이터셋의 대여소명 형식이 달라 병합 실패
- **해결:** 정규표현식과 문자열 처리로 형식 통일

### **이슈 2: 인코딩 오류**
- **문제:** CSV 파일 인코딩이 cp949와 utf-8 혼재
- **해결:** try-except 구문으로 자동 인코딩 감지 함수 작성

---

## **6. ✅ 중간보고 요약**

- ✅ **1단계 완료:** 서울 열린데이터에서 따릉이 데이터 수집
- ✅ **2단계 완료:** 데이터 정제 및 병합 → `seoul_bike_final_data.csv` 확보
- 🔲 **3~6단계 진행 예정:** 피처 엔지니어링 → 머신러닝 → 시각화 → 인사이트 도출

**현재 진행률:** 약 30% (데이터 준비 단계 완료, 분석 준비 완료)
